interface:
  display_name: "Execute Plan"
  short_description: "Execute approved plans in verified stages"
  default_prompt: "Use $execute-plan to implement the next authorized stage of this approved plan and verify it."

policy:
  allow_implicit_invocation: true
