interface:
  display_name: "Plan Code Change"
  short_description: "Create implementation-ready change plans"
  default_prompt: "Use $plan-code-change to produce an evidence-backed implementation plan for this change."

policy:
  allow_implicit_invocation: true
