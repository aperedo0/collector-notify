interface:
  display_name: "Post Edits"
  short_description: "Review and fix uncommitted work"
  default_prompt: "Use $post-edits to review and repair the current uncommitted changes."

policy:
  allow_implicit_invocation: true
